---
name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - name: Install uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39  # v8.2.0

      - name: Lint
        run: |
          uv run --frozen --extra dev ruff check .
          uv run --frozen --extra dev ruff format --check .

  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - name: Install uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39  # v8.2.0

      - name: Type check
        run: uv run --frozen --extra typecheck mypy

  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - name: Install uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39  # v8.2.0

      - name: Test
        run: uv run --frozen --extra test --python ${{ matrix.python-version }} pytest

  release:
    needs: [lint, typecheck, test]
    if: github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
    runs-on: ubuntu-latest
    environment: release
    permissions:
      id-token: write  # PyPI trusted publishing (OIDC)
      contents: write  # version commit, tag, and GitHub release
    steps:
      # semantic-release pushes the version commit and tag back to main. The
      # default GITHUB_TOKEN cannot push to a protected branch, so if main is
      # protected, add a RELEASE_TOKEN secret (a GitHub App or PAT allowed to
      # bypass protection); it is used automatically when present.
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0
        with:
          fetch-depth: 0
          token: ${{ secrets.RELEASE_TOKEN || secrets.GITHUB_TOKEN }}

      - name: Install uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39  # v8.2.0

      # On a push, semantic-release determines the next version, commits the
      # bump, tags it, creates the GitHub release, and populates dist/.
      # On a workflow_dispatch retry, the tag already exists so semantic-release
      # is a no-op; we build directly from the current tag instead.
      - name: Semantic release (version + tag + GitHub release)
        if: github.event_name == 'push'
        env:
          GH_TOKEN: ${{ secrets.RELEASE_TOKEN || secrets.GITHUB_TOKEN }}
        run: uv run --frozen --extra release semantic-release version

      - name: Build (publish retry)
        if: github.event_name == 'workflow_dispatch'
        run: uv run --extra release python -m build

      - name: Check for distribution artifacts
        id: check_dist
        run: |
          if [ -d dist ] && [ -n "$(ls -A dist)" ]; then
            echo "has_dist=true" >> "$GITHUB_OUTPUT"
            ls -la dist/
          else
            echo "has_dist=false" >> "$GITHUB_OUTPUT"
            echo "No artifacts — nothing to publish."
          fi

      - name: Publish to PyPI
        if: steps.check_dist.outputs.has_dist == 'true'
        uses: pypa/gh-action-pypi-publish@cef221092ed1bacb1cc03d23a2d87d1d172e277b  # v1.14.0
        with:
          attestations: false
